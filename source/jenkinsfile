pipeline {
    agent any

    stages {
        stage('Ejecutar Docker --version') {
            steps {
                script {
                    // Ejecutar el comando 'docker --version'
                    sh 'docker create --name mi_contenedor ubuntu:latest'
                }
            }
        }
    }
}

tubería {
    agente any

    etapas {
        etapa('Descargar Imágenes Docker') {
            pasos {
                guión {
                    def imagenes = ['ubuntu','nginx','mongo','redis']
                    imagenes.each { imagen ->
                        sh "docker pull ${imagen}:latest"
                    }
                }
            }
        }
    }
}

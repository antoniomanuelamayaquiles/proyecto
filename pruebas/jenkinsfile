pipeline {
    agent any

    stages {
        stage('prueba') {
            steps {
                script {
                    sh "docker --version"
                }
            }
        }
    }
}
